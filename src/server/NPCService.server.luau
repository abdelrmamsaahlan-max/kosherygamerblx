-- NPC behavior is now owned by init.server.luau so customers and the chef cannot fight over movement or animations.
-- Kept as a compatibility script so existing Rojo project trees do not need manual cleanup.
print("EgyptianFood NPCService: core NPC controller is active")
