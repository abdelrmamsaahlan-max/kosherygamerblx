-- Customer movement, spawning, skins, prompts and walking animations are now handled by init.server.luau.
-- This compatibility script intentionally does not create a second NPC system.
print("EgyptianFood CustomerNPCService: core customer controller is active")
