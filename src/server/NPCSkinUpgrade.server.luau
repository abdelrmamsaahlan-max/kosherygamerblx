-- NPC skins, uniforms and animation tracks are now created by init.server.luau.
-- This compatibility script stays passive to prevent duplicate accessories and animation tracks.
print("EgyptianFood NPCSkinUpgrade: core NPC styling is active")
